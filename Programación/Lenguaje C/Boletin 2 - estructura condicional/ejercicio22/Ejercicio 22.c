#include <stdio.h>

void main(){

    int dia_ano;
    int mes_ano;

    printf("Dime el dia del año: ");
    scanf("%i",&dia_ano);
    fflush(stdin);

    printf("Dime el mes del año: ");
    scanf("%i",&mes_ano;
    fflush(stdin);

    if (mes_ano >= 3 || mes_ano <=4){
        if (dia_ano )
    }

}